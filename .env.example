# Optional: allow another device on your local network to load Next.js dev assets.
# Use the hostname or IP address you open in the browser, without protocol or port.
# Example:
# NEXT_ALLOWED_DEV_ORIGINS=192.168.1.23
NEXT_ALLOWED_DEV_ORIGINS=
