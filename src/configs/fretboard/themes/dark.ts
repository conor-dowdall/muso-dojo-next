import type { FretboardTheme } from "@/types/fretboard";

const dark: FretboardTheme = {
  fretWireColor: "#BBB",
  fretWireWidth: "2px",
  stringColor: "#ddd",
  stringWidth: "1px",
};

export default dark;
