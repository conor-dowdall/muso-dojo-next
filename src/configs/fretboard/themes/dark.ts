import type { FretboardTheme } from "@/types/fretboard";

const dark: FretboardTheme = {
  fretWireColor: "#555",
  fretWireWidth: "3px",
  stringColor: "#ddd",
  stringWidth: "1px",
};

export default dark;
