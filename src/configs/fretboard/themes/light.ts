import type { FretboardTheme } from "@/types/fretboard";

const light: FretboardTheme = {
  fretWireColor: "#999",
  fretWireWidth: "1px",
  stringColor: "#333",
  stringWidth: "1px",
};

export default light;
